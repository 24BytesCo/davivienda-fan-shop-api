ENV=development

# Puerto publicado en el host (Docker Compose mapea ${API_PORT}:3000)
API_PORT=3000

# Base de datos
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=davivienda_fan_shop

# JWT
JWT_SECRET=un_secreto_seguro
JWT_EXPIRES_IN=1d

# Firebase
# Nombre de bucket, p. ej.: tu-proyecto.appspot.com
FIREBASE_STORAGE_BUCKET=

# Semilla de usuario inicial (opcional; solo si no hay usuarios)
# ADMIN_ROLE puede ser: administrador | editor | usuario
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_FULLNAME=
ADMIN_ROLE=administrador

